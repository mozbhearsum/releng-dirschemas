# For use with Python projects that use pyup. Generally, any Python application or tool should be.
---
allow_extra_files: true
allow_extra_dirs: true
files:
  ".pyup.yml":
    contains:
    - "update: all"
    - "pin: True"

